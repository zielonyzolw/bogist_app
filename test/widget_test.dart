import 'package:flutter_test/flutter_test.dart';

// BLE plugins require a real device; no widget tests for the MVP.
void main() {
  testWidgets('placeholder', (tester) async {
    expect(true, isTrue);
  });
}
